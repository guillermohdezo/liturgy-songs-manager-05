{
  "buildCommand": "cd api && npm ci --only=prod && npm install --save-dev typescript && npm run build",
  "outputDirectory": "api/dist",
  "functions": {
    "api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
