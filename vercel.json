{
  "buildCommand": "cd api && npm install && npm install --save-dev @types/express @types/cors @types/jsonwebtoken @types/multer && npm run build",
  "outputDirectory": "api/dist",
  "functions": {
    "api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
